machine:
  node:
    version: 6.0.0
dependencies:
  cache_directories:
    - "client/bower_components"
  post:
    - bower install
deployment:
  prod:
    branch: master
    commands:
      - gulp build
      - gulp buildcontrol:heroku
